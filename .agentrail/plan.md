# Saga: file-date-library

Implement the reusable pure MLPL date-metadata library needed by downstream consumers without building or modifying peer repositories.

## Steps
1. date-index-library -- TDD exact Unix-ms UTC conversion/formatting, metadata permutation sorting, tie/unavailable policy, public documentation, and a synthetic self-describing demo; retain the thin live file_metadata adapter as explicitly blocked until a rebuilt interpreter is supplied.