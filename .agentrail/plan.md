1. Validate compiled CLI argument, stdin/stdout/stderr, exit-status, and error conformance against interpreter behavior.\n2. Compile hexdump and histogram applications and verify interpreter/compiler parity.\n3. Compile a WAV or Ogg inspector/copy tool with binary I/O parity.\n4. Audit a clean-environment artifact to prove no source, parser, REPL, or interpreter dependency.\n5. Publish the standalone application capability report and reconcile repository documentation.