Implemented a bounded byte histogram that traverses immutable range-reader state and merges one MLPL-owned 256-bin reduction per chunk without retaining prior chunks. Added a runnable cataloged demo and mlplunit parity tests for chunk sizes 1, 7, 64, and 65536 over empty, repeated, boundary, and all-byte fixtures. Documented O(256n) work, O(256c + 256) numeric payload and immutable accumulator copies, while reserving peak-RSS claims for the measurement step. The full just check gate passes with 11 native suites.