Implemented a bounded MPEG Layer III scanner with deterministic two-header acquisition, byte-wise resynchronization, decoded frame skipping, synchronized truncation handling, explicit byte/frame budgets, and fixed-size duration and bitrate statistics. Added native mlplunit coverage for isolated false sync, damage recovery, chunk invariance, CBR/VBR statistics, range errors, truncation, and budgets; documented the contract and capability boundary. Full just check gate passed.