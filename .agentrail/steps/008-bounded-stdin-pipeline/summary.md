Replaced the whole-input compiled wc filter with a genuine read_stdin_chunk pipeline; preserved cross-read ASCII word and UTF-8 byte semantics; enforced a 64 MiB total budget; added CLI help, useful exit statuses, read/stdout Result propagation, host/source-free/budget/closed-pipe artifact checks, and mlplunit budget coverage; measured identical 2,080,768-byte peak RSS for complete 1 MiB and 64 MiB runs; and updated all capability, plan, saga, contract, and demo documentation.