Implement reusable MLPL endian integer decode/encode helpers and data-described bit-field extraction, including fields that cross byte boundaries. Add golden native mlplunit vectors for little/big endian widths, round trips, exact-integer boundaries, invalid widths/ranges, and MPEG-style header fields. Keep generic format logic out of the runtime unless executable evidence proves it cannot be expressed. Update demos, catalogs, complexity/capability documentation, run just check, commit before agentrail complete, push main, then stop.