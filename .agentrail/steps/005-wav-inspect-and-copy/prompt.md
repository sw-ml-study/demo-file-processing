Generate tiny redistributable PCM WAV fixtures and implement MLPL inspection plus canonical decode/encode copy using the shared byte/endian helpers. Add native mlplunit tests for valid empty/minimal audio, odd padding, truncated chunks, inconsistent lengths, unsupported encoding/channels/widths, unknown chunks, overflow budgets, semantic round trips, and canonical byte-identical output. Document whole-file memory/copy limits and never claim bounded streaming. Update catalogs and README, run just check, commit before agentrail complete, push main, then stop.