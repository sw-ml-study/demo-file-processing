Run the complete repository suite and produce the foundation acceptance report. Summarize executable evidence for byte representation, whole-file I/O, bit/endian/field operations, WAV round trips, mlplunit adoption, CLI/compiler coverage, and interpreter/compiler parity. Write the smallest evidence-backed bounded I/O, packed-byte, application-runtime, and compiler contracts needed for later sagas without modifying ../sw-mlpl. Reconcile docs/plan.md and docs/sagas.md with findings, decide whether bounded-file-streaming is unblocked, run just check, commit before agentrail complete with --done, push main, then stop.