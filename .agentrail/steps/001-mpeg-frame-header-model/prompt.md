Turn the existing golden MPEG bit-field demonstration into a reusable semantic MPEG audio frame-header parser. Use authoritative MPEG audio references already present locally if available; otherwise browse primary specifications or authoritative tables. Model sync, MPEG version, layer, protection, bitrate/sample-rate indices, padding, channel mode, mode extension, copyright/original/emphasis, derived bitrate/sample rate/frame samples/frame length, and reserved/free-bitrate policy. Keep all substantive parsing and decisions in MLPL over the existing declarative fields and bounded-safe integer domain; do not add a native MPEG parser. Use TDD through mlplunit with golden vectors spanning supported MPEG-1/2/2.5 Layer III cases and malformed/reserved combinations. Upgrade the narrated MPEG demo to explain at least two meaningfully contrasting headers and their derived frame sizes. Update catalog/docs and cite sources precisely. Run just check, commit named implementation files with detailed messages, complete Agentrail metadata separately, push origin main, and report blockers and the next scanner step.
