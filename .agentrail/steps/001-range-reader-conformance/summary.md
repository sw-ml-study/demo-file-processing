Implemented and documented immutable bounded range-reader state over file_size and ranged read_bytes. Added four mlplunit conformance tests covering required chunk budgets, empty/EOF/clamped reads, exact-domain validation and overflow, missing/directory/sandbox errors, and runtime ownership. The full just check gate passes with 10 native suites. Also responded to the requested histogram improvement by showing four contrasting compact outputs. Fixed the live-doc audit to ignore append-only Agentrail archives and regenerated the new saga instruction metadata.