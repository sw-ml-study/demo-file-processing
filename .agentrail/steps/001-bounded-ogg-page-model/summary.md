Implemented a bounded version-zero Ogg page model with capture/version/flag validation, lacing and body extent checks, strict page/segment/body budgets, body and next-page descriptors, packet-ending counts, stored checksum fields, and exact split-word 64-bit granule positions including the unset sentinel. Added native mlplunit coverage for multi-segment, empty, malformed, budget, and chunk-invariant cases; documented the contract and capability boundary. Full just check passed with 18 suites.