Measured the configured interpreter/compiler boundary with native mlplunit and process-level probes. Verified whole-file and bounded range reads, file_size, Result byte writes, f64-backed arrays, fixed-width bit operations, CLI args/text stdin/stdout/stderr/exit, and simple native numeric parity. Corrected the roadmap because bounded reads are shipped. Recorded blockers: incremental binary sinks/stream state are absent; read_bytes, args, and band are not compiler-lowered; documented arithmetic compilation fails from a missing ApplyBinopExt import. These do not block Step 003. Full just check passes. Next highest-value unblocked step: 003-hexdump-and-histogram.