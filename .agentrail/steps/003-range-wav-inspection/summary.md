Implemented read-only bounded RIFF/WAV inspection using explicitly budgeted range windows. The parser reads only RIFF, chunk, and 16-byte PCM format headers, validates lengths and odd padding, records data offsets and sizes, and skips sample and unknown payloads without retaining them. Added a runnable cataloged demo plus 6 mlplunit tests proving whole-buffer metadata parity for all fixtures, every header split at budgets 1 through 16, clamping budgets 64 and 65536, and malformed/budget/window errors. Documented ownership, constant metadata state, header allocation bounds, concurrency behavior, and excluded copy/write/RSS claims. Full just check passes with 13 native suites.