Implemented checksum-verified bounded Ogg page copy and descriptor-driven sequence rewriting with streamed virtual CRC calculation, bounded append substitution across header splits, exact u32/budget/count/size invariants, and partial cleanup. Added four mlplunit cases for byte identity, payload preservation, CRC integrity, chunk boundaries, corruption and unsafe outputs; added two narrated artifacts and reconciled Ogg docs. Full just check passed with 26 native suites.