Added four checksum-pinned Ogg fixtures: MLPL-generated structural cross-page, continuation-invalid, and CRC-corrupt cases plus a decodable 440 Hz Ogg/Opus tone. Added MLPL regeneration and fixture audits, chunk-invariant fixture/CRC/packet mlplunit evidence, and self-describing page, packet, and CRC demos. Added an opt-in ffprobe 8.1.2 oracle explaining fifteen structural packets versus thirteen decoded Opus packets. The installed FFmpeg lacked libvorbis, so the oracle is explicitly Ogg/Opus; this does not block codec-independent container validation. Full just check passed with 21 suites.