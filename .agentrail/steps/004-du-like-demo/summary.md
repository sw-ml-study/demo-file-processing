Added and accepted a confined du-like logical-size application with deterministic lexical rows, exact root/entry/depth/sum validation, explicit symlink and apparent-size policy, native mlplunit coverage, a narrated six-fixture demo, and an independent 573-byte host wc oracle. Added compiled wc/grep/du probes and advanced all application compiler change detectors from eq/2 to the current tally/1 boundary. Full just check passes 120 tests across 35 suites.