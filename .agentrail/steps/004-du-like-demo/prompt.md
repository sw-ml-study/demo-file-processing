Build a confined du-like MLPL application using the available generic traversal and file-size surfaces. Enforce root, entry, depth, and byte-sum budgets; define symlink/error policy and deterministic ordering; distinguish logical byte totals from allocated blocks; add mlplunit and host-oracle coverage, self-describing outputs, catalog/docs, and full gate. Do not modify or build peer repositories.