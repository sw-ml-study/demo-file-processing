Implemented the Ogg non-reflected CRC-32 polynomial 0x04C11DB7 visibly in MLPL with initial zero/no final xor. Added bounded page calculation that logically zeroes checksum bytes 22-25, explicit inspect-versus-require verification APIs, and CRC byte/chunk budgets. Added independent empty/OggS/123456789 golden vectors, stored-page chunk invariance, body corruption mismatch, invalid input, and budget coverage. Documented ownership and O(page bytes times eight) cost. Full just check passed with 20 suites.