After upstream ships an interpreter-and-compiler-parity byte-oriented incremental stdin primitive, replace the whole-input compiled wc filter with a bounded pipeline. Require read_stdin_chunk(max_bytes) or equivalent to return at most max_bytes plus explicit EOF, preserve cross-chunk word state, enforce a total byte budget, handle empty input and UTF-8 byte splits, propagate read/write errors and exit status, compare with host wc, and measure 1 MiB versus 64 MiB peak RSS before claiming O(chunk_size) memory. Do not simulate streaming by slicing read_stdin() output and do not modify or build peer repositories.