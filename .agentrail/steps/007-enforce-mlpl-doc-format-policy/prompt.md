Strengthen the repository's mandatory agent instructions so every user-defined function in every .mlpl file begins with a meaningful doc string, and every .mlpl file is formatted with ../sw-mlpl/scripts/mlpl-fmt.sh before each commit and again before each push. Keep AGENTS.md as the sole instruction target and do not create CLAUDE.md. Validate instruction consistency and the repository gate, then commit and push the named files.