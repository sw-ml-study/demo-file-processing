Added an opt-in sparse-file peak-memory harness for bounded histogram and WAV inspection. It generates deterministic sparse artifacts, measures fresh MLPL processes with Darwin/Linux platform time, and enforces 32 MiB RSS plus 8 MiB growth ceilings. Recorded histogram RSS of 20955136 and 20381696 bytes across 64 KiB and 1 MiB inputs, and identical 10027008-byte WAV RSS across 1 MiB and 64 MiB payloads. Full just check passes with 13 mlplunit suites. Restricted macOS metrics were an initial blocker resolved by authorized execution; the remaining limitation is the pedagogical histogram's slow O(256n) CPU cost, not memory growth. Inserted the user-requested self-describing-demo-output step before the final report and narrated the sparse demos immediately.