Implement the smallest reusable MLPL bounded range-reader state/helper over file_size and read_bytes(path, offset, length). Add mlplunit conformance tests for chunk sizes 1, 7, 64, and 65,536; empty files; zero-length reads; EOF clamping; beyond-EOF offsets; invalid offsets and lengths; overflow; missing or denied paths; and sandbox escape behavior supported by the current runtime. Keep f64 exact-offset limits and runtime ownership explicit. Update relevant documentation and catalog entries. Run just check, commit implementation files with named git add and a detailed commit, then complete the Agentrail step in a separate metadata commit and push origin main. Report blockers and the next highest-value step.
