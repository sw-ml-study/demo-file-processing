Implement reusable MLPL whole-buffer byte formatting and histogram logic plus readable self-checking hexdump and byte-histogram demos over tiny generated fixtures. Add native mlplunit suites using shared assertions for empty, singleton, all-byte-range, repeated-byte, formatting-boundary, and invalid-input cases. State MLPL/runtime ownership, logical complexity, physical representation, and copy/allocation behavior; do not claim streaming. Update catalogs and docs, run just check, commit before agentrail complete, push main, then stop.