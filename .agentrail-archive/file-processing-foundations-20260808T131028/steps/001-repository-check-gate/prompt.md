Create the initial repository validation workflow. Add a thin justfile delegating to portable scripts; deterministic license, README/documentation-link, source, fixture, catalog, gitignore, and Agentrail checks; documented MLPL and mlplunit binary selection without installation; a root mlplunit.conf; human, TAP, list, path, and filter test wrapper modes; native @test/shared-assertion/run_registered_tests conventions; and an adoption audit that rejects ad hoc executable MLPL tests. Use TDD for executable checks, run the full gate, update user documentation, commit named files and durable Agentrail metadata before agentrail complete, push main using only git push origin main, then stop.