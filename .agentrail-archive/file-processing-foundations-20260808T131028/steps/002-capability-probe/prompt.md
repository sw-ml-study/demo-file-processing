Add executable deterministic probes against the configured sw-MLPL interpreter and compiler for whole-file byte read/write behavior and physical representation, fixed-width bit operations, exact-integer limits, bounded range/seek/stream APIs, CLI arguments and process I/O, native build lowering, runtime dependencies, and interpreter/compiler parity. Express every executable MLPL assertion as native mlplunit tests. Record observed versions and capabilities without claiming streaming, packed bytes, or standalone application support prematurely. Run just check, update capability and upstream-contract documentation, commit before agentrail complete, push main, then stop.