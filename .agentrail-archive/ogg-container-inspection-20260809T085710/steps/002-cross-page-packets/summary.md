Implemented bounded packet-boundary reconstruction for one logical Ogg stream. The fold validates BOS/EOS placement, stable serial, u32 sequence progression, continuation agreement, stream/page/segment/body/packet budgets, and requested extents while retaining no payloads or packet list. It reports fixed-size packet and spanning statistics and handles empty streams. Added native mlplunit coverage for a 265-byte cross-page packet, zero/ordinary packets, all required chunk sizes, malformed continuation/identity/sequence/end state, truncation, and budgets. Full just check passed with 19 suites.