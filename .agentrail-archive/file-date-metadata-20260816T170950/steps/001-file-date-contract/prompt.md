Document the demo-extensions timestamp handoff in demo-file-processing. Add the smallest evidence-backed sw-mlpl contract for confined file_metadata or file_modified_ms returning exact UTC Unix milliseconds with explicit unavailable errors and unchanged sandbox/symlink protection. Plan a self-describing MLPL demo and mlplunit coverage for directory/date scanning inputs, deterministic timestamp sorting/ties, UTC formatting, unavailable timestamps, precision, and platform parity. Update docs/plan.md, docs/sagas.md, docs/upstream-contract.md, and relevant capability/catalog references without claiming the unavailable runtime API. Run just check, commit, complete, and push.