Recorded the confined file_metadata(path).modified_unix_ms downstream contract and a gated file-date-index plan covering explicit bounded paths, deterministic sorting/ties, pure UTC formatting, unavailable timestamps, exactness, confinement, and macOS/Linux parity. Verified upstream implementation is active but uncommitted and absent from current interpreters. During the full gate, refreshed compiler probes and reports for newly shipped read/append/bit/control-flow/Result support and advanced real applications to the eq/2 boundary. Full just check passed.