Write a concise standalone Markdown handoff for the sw-mlpl repository agent. Explain what interpreter behavior is accepted, why standalone applications are not accepted, exact observed compiler failures and semantic mismatches, ordered upstream work, non-blocking later items, reproduction commands, and the precise downstream resume trigger. Link it from relevant documentation and run the full gate.