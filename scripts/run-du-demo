#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=$("$repo_root/scripts/select-mlpl")

cd "$repo_root"
exec "$mlpl" --source-dir "$repo_root" "$repo_root/demos/files/du.mlpl"
