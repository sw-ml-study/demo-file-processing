#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=$("$repo_root/scripts/select-mlpl")
path=${1:-fixtures/mp3/synthetic-vbr-id3.mp3}
exec "$mlpl" --source-dir "$repo_root" "$repo_root/demos/apps/media_inspector.mlpl" -- "$path"
